name: Build and deploy

# Deploys to GitHub Pages on every push to main or update-ltg (one Pages
# environment; the most recent push wins — an update-ltg push temporarily
# replaces the live site with the in-progress LTG refinement until main is
# pushed again). Manual runs allowed.
on:
  push:
    branches: [main, update-ltg]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Let the latest deploy supersede an in-flight one rather than queueing.
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout (with the generator submodule)
        uses: actions/checkout@v7
        with:
          submodules: recursive

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - name: Install site dependencies
        run: npm ci

      - name: Install generator dependencies (for yeoman-test)
        # build:snippets runs the real generator out of the submodule to source
        # the canonical preamble, so it needs the generator's own node_modules.
        run: npm ci
        working-directory: generator-latex-template

      - name: Pre-pull TeX Live image
        run: docker pull texlive/texlive:latest

      - name: Build snippets (LaTeX → SVG via Docker) + site
        run: npm run build

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: build-site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
